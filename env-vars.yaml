GCP_PROJECT_ID: validatus-platform
# DATABASE_URL should be injected via GCP Secret Manager or secure environment variables
# Never commit actual credentials to version control
DATABASE_URL: postgresql+asyncpg://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:5432/validatusdb
ENVIRONMENT: production
ALLOWED_ORIGINS: https://validatus-frontend-${GCP_PROJECT_ID}.run.app,https://validatus-backend-${GCP_PROJECT_ID}.run.app
